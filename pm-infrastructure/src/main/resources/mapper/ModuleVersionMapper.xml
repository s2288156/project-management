<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pm.infrastructure.mapper.ModuleVersionMapper">
    <select id="selectByMidAndVersion" resultType="com.pm.infrastructure.dataobject.ModuleVersionDO">
        select id, create_time, update_time, mid, version, description
        from t_module_version
        where mid = #{mid}
          and version = #{version}
    </select>
    <select id="listModuleVersion" resultType="com.pm.infrastructure.dataobject.ModuleVersionDO">
        select id, create_time, version, description
        from t_module_version
        where mid = #{mid}
    </select>
    <select id="selectModuleVersionIdByProjectId" resultType="java.lang.String">
        select tmv.id
        from t_module_version tmv
        where EXISTS(select tm.id mid
                     from t_project tp
                              left join t_module tm on tp.id = tm.pid
                     where tp.id = #{pid}
                       and tmv.mid = tm.id)
    </select>
</mapper>
